// Select color input
// Select size input

// When size is submitted by the user, call makeGrid()
let height, width, color, submit, table, d;

function makeGrid() {
    d = document;
    height = $("#input_height").val();
    width = $("#input_width").val();
    color =  $("#colorPicker").val();
    submit = $("#submit");
    table = $("#pixel_canvas");
for (let x = 0; x < height; x++){
    for(let y = 0;y < width; y++){

    }
}
}